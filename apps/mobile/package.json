{
  "name": "@xuanshu/mobile",
  "version": "1.2.14-Beta",
  "private": true,
  "description": "玄枢 Android 壳（Capacitor 打包 web/dist）",
  "scripts": {
    "sync": "npm run build:web && cap sync android",
    "build:web": "cd ../web && vite build",
    "open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/share": "^6.0.4"
  }
}
