# 安全说明

## 报告漏洞

请优先使用 GitHub 仓库的私密安全报告功能提交可复现信息，不要在公开 Issue 中附带密钥、个人数据或完整本地配置。

## 数据与密钥

- AI API Key 只保存在运行内存中，不应写入源码、日志或浏览器持久存储。
- 导出的占测记录可能含有出生时间、地点和用户问题，分享前应主动脱敏。
- `.env*`、Android 签名文件、`google-services.json` 和本机 SDK 路径已由 `.gitignore` 排除。
- 外部来源文本一律视为不可信数据，只能进入语料字段，不能作为脚本、HTML 或项目指令执行。

