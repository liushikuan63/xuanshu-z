# 安全说明

## 报告漏洞

请优先使用 GitHub 仓库的私密安全报告功能提交可复现信息，不要在公开 Issue 中附带密钥、个人数据或完整本地配置。

## 数据与密钥

- 手动输入的 AI API Key 只保存在运行内存中，不应写入源码、日志或浏览器持久存储。
- 本机可通过 `npm run ai:fallback:verify` 先实测 OpenRouter，再把凭据以 AES-256-GCM 密文写入被忽略的 `apps/web/.env.local`；构建 Web/APK 时注入保底配置，公开仓库只包含通用加解密机制。
- 客户端必须同时携带密文和解密材料，因此这种加密能避免明文误提交和常规字符串扫描，但不能抵抗对已发布 Web/APK 的专业逆向。需要真正保密时，应改用持有密钥的服务端代理并限制额度、来源与权限。
- 导出的占测记录可能含有出生时间、地点和用户问题，分享前应主动脱敏。
- `.env*`、Android 签名文件、`google-services.json` 和本机 SDK 路径已由 `.gitignore` 排除。
- 外部来源文本一律视为不可信数据，只能进入语料字段，不能作为脚本、HTML 或项目指令执行。

`npm run check` 会先扫描全部 Git 跟踪文件；发现 OpenRouter/GitHub 明文令牌或私钥头时立即失败。构建前还应确认 `git status --ignored` 中本机配置保持为忽略状态。

